#wait
#C语言 
[[202112132225]]fork之后，如果父进程需要等待子进程执行完毕，就使用wait来操作。

样例如下
```c
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/wait.h>

int main(int argc, char *argv[]){
    printf("hello wolrd (pid:%d)\n",(int)getpid());
    int rc = fork();
    if(rc < 0){
        fprintf(stderr,"fork failed\n");
        exit(1);
    }
    else if (rc == 0){
        printf("hello , I am child (pid:%d)\n",(int)getpid());
    }else{
        int wc = wait(NULL);
        printf("hello, i am parent of %d (wc:%d), (pid:%d)\n",rc,wc ,(int)getpid());
    }
    return 0;
}
```

```
➜   gcc p2.c
➜   ./a.out
hello wolrd (pid:8894)
hello , I am child (pid:8895)
hello, i am parent of 8895 (wc:8895), (pid:8894)
```

